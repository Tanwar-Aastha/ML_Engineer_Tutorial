## Pydantic

Steps: -
1. Define  pydantic class representing the `ideal schema` of the data.
2. Create a raw data(dictionary/JSON) and pass it to the class object.
3. At last pass the validated class to the function in FastAPI.
